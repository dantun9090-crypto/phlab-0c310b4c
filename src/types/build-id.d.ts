// Injected at build time by vite.config.ts via `define`. Changes on every
// Lovable Publish — used to fire post-publish cache invalidation once per
// new deployment.
declare const __BUILD_ID__: string;
